```dataviewjs
let now = dv.date("today");
let start = now.startOf("month");
let end = now.endOf("month");

let pages = dv.pages('"LIFE/Дневники"')
  .where(p => p.file.name >= start.toFormat("yyyy-MM-dd") 
           && p.file.name <= end.toFormat("yyyy-MM-dd"));

let grouped = pages.groupBy(p => p.type);

dv.table(["Тип", "Количество"],
  grouped.map(g => [g.key, g.rows.length])
);
```


## ==всего за год==

```dataviewjs
let pages = dv.pages('"LIFE/Дневники"');

let grouped = pages.groupBy(p => p.type);

dv.table(["Тип", "Количество"],
  grouped.map(g => [g.key, g.rows.length])
);
```
```dataviewjs
let now = dv.date("today");
let start = now.startOf("month");
let end = now.endOf("month");

let pages = dv.pages('"LIFE/Дневники"')
  .where(p => 
    p.file.name >= start.toFormat("yyyy-MM-dd") &&
    p.file.name <= end.toFormat("yyyy-MM-dd") &&
    p.type !== "day"
  );

dv.paragraph("Всего: " + pages.length);
```